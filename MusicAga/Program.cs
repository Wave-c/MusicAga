using MusicAga.Models;

namespace MusicAga
{
    internal class Program
    {
        static void Main(string[] args)
        {
            //DataContextFactory factory = new DataContextFactory();
            //AppDbContext.LoadContext(factory.CreateContext("data.json"));
            //var tmp = AppDbContext.GetContext();
            //Console.WriteLine();
            //AppDbContext.GetContext().accessories.Add(new Models.Accessories.Accessory("aga", "uwu", (decimal)52.42, "42CORE", 1488, "qwe", "a"));
            //AppDbContext.GetContext().SaveContext();
        }
    }
}
